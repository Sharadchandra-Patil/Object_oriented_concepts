using namespace std;
#include<iostream>




/*
class Complex
{
    public:
    int real; // data member of class Complex
    int imag; // data member of class Complex

    void accept() // member function of class Complex
    {
        cout<<"Enter Real number :";
        cin>>real;
        cout<<"Enter Imaginary value :";
        cin>>imag;
    }

    void disp() //member function of class Complex
    {
        cout<<"\n Real Value : "<<real<<" Imag Value : "<<imag;
    }

};
int main(void)
{
    //object of the class
    // classname object_name;
    Complex c1; // c1 is object of class Complex
    c1.accept(); // accept() function is called upon c1 object
    cout<<"\n displaying c1 data...";
    c1.disp(); // disp() function is called upon c1 object

    Complex c2; // c2 is object of class Complex
    c2.real=50;
    c2.imag=30;
    cout<<"\n displaying c2 data ...";
    c2.disp();   // 50 30

    c2.imag=c1.imag; //change the value of one object element with other object element
    cout<<"\n again displaying c2 data...";
    c2.disp(); // 50    10  




    return 0;
}

*/





/*
class Complex
{
    public:
    int real; // data member of class Complex
    int imag; // data member of class Complex

    void accept() // member function of class Complex
    {
        cout<<"Enter Real number :";
        cin>>real;
        cout<<"Enter Imaginary value :";
        cin>>imag;
    }

    void disp() //member function of class Complex
    {
        cout<<"\n Real Value : "<<real<<" Imag Value : "<<imag;
    }

};
int main(void)
{
    //object of the class
    // classname object_name;
    Complex c1; // c1 is object of class Complex
    c1.accept(); // accept() function is called upon c1 object
    c1.disp(); // disp() function is called upon c1 object

    Complex c2; // c2 is object of class Complex
    c2.real=50;
    c2.imag=30;
    c2.disp();

    return 0;
}
*/






//class classname
//{
       // ----- 
//}

//class is collection of data members and member functions
// Binding of data + code toghther is called as ENCAPSULATION
// Class is one example of ENCAPSULATION



/*
// private and public are access specifiers 

// By default class members are private 
// By default structure members are public
//private indicates we can not access the members outside the class

class Complex
{
    int real; // data member of class Complex
    int imag; // data member of class Complex

    void accept() // member function of class Complex
    {
        cout<<"Enter Real number :";
        cin>>real;
        cout<<"Enter Imaginary value :";
        cin>>imag;
    }

    void disp() //member function of class Complex
    {
        cout<<"\n Real Value : "<<real<<" Imag Value : "<<imag;
    }

};
int main(void)
{
    //object of the class
    // classname object_name;
    Complex c1; // c1 is object of class Complex
    c1.accept(); // accept() function is called upon c1 object
    c1.disp(); // disp() function is called upon c1 object

    return 0;
}

*/